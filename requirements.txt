fastapi>=0.136
uvicorn[standard]>=0.49
python-multipart>=0.0.9
google-cloud-bigquery>=3.41
google-genai>=1.0
pypdf>=6.0
python-docx>=1.1
pymupdf>=1.26
google-cloud-vision>=3.7
openpyxl>=3.1
xlrd>=2.0
# Nota: la extraccion de hojas de calculo usa openpyxl (.xlsx) / xlrd (.xls) /
# csv stdlib (.csv) -> Tabla Markdown. Se omite pandas a proposito: no aporta
# para celdas->Markdown y sus wheels en Python 3.14 son inmaduros.
# .doc binario (Word 97-2003) NO es legible por python-docx; convertir a .docx/PDF.
